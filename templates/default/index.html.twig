{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}
    <div class="starter-template">
      <h1 class="display-1">PetShop</h1>
      <p class="lead">Avançando com Symfony</p>
    </div>
    <div class="row">
        <div class="col-md-6">
            <table class="table table-striped">
                {% for qts in qts_animais %}
                    <tr>
                        <th>{{ qts.nome }}</th>
                        <td>{{ qts.qtde }}</td>
                    </tr>
                {% endfor %}
            </table>
        </div>
        <div class="col-md-6">
            <table class="table table-striped">
                {% for qts in qted_por_raca %}
                    <tr>
                        <th>{{ qts.nome }}</th>
                        <td>{{ qts.qtde }}</td>
                    </tr>
                {% endfor %}
            </table>
        </div>
</div>
{% endblock %}
